# LogistiX (Odoo Custom Addon) Dependencies
# Core Odoo dependencies are expected to be installed globally or via the main Odoo requirements.txt
# Add custom python libraries required by LogistiX here.
psycopg2-binary>=2.9.0
requests>=2.28.0
